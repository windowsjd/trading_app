--- HEAD tracked inlineSchema
+++ canonical generated inlineSchema
@@ -1042,11 +1042,10 @@
   currencyCode         CurrencyCode? @map("currency_code")
   quotedPrice          Decimal?      @map("quoted_price") @db.Decimal(24, 8)
   quotedRate           Decimal?      @map("quoted_rate") @db.Decimal(18, 8)
-  // Fee basis pinned AT QUOTE TIME for limit orders and general market orders
-  // (season market / FX quotes remain null). Limit-buy create reserves
-  // quotedReservedAmount; limit-sell create reserves quantity and uses
-  // quotedNetAmount. General market price still reprices at execute, while the
-  // fee rate stays fixed across rolling deploy/config changes.
+  // Fee rate pinned AT QUOTE TIME for new market, limit and FX quotes in both
+  // account modes. Nullable only for legacy compatibility. Market/FX amounts
+  // still use the execute price/rate. Limit-buy reserves quotedReservedAmount;
+  // limit-sell reserves quantity and uses quotedNetAmount.
   quotedFeeRate        Decimal?      @map("quoted_fee_rate") @db.Decimal(10, 6)
   quotedGrossAmount    Decimal?      @map("quoted_gross_amount") @db.Decimal(24, 8)
   quotedFeeAmount      Decimal?      @map("quoted_fee_amount") @db.Decimal(24, 8)
