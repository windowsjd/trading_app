# 실제 흐름 목록 — 본 조사 전 등록

기준: `dc138a3ffede091c32b17359ec79585ec7f32e9c`, 시작 working tree clean. 2026-09-22.

원 요청 F01~F29를 유지한다. 프로필 수정은 F29/F02, health/readiness·CLI는 F28, 운영자 보상은 F26/F27로 연결한다. 모든 HTTP 금융 경로는 `/api/v1`이다. 아래 목록을 먼저 등록하고 F01부터 순서대로 본 조사를 진행했다. 최종 검토에서 실제 class명·연결을 교정했으며, 결과는 [보고서](report.md)의 B/C/G/H에 연결했다.

공통 판정: 금융 소유권의 기준 데이터(source of truth)는 PostgreSQL의 User↔TradingAccount 관계이다. UI 선택·Redis·WebSocket은 권한 근거가 아니다. 실제 배포 flag와 운영 성공 여부는 미확인이다. 시작/완료 조건은 각 제목의 첫/마지막 단계이며, 정상·차단·실패·복구·테스트 실행 결과는 본 보고서 C에 기록한다. 테스트 열은 관련 파일군을 뜻하며 실행 PASS를 의미하지 않는다.

| ID·목적(시작 → 완료) | 진입점·주 호출 연결 | 저장 데이터·판정 기준 | 적용 | 금융 쓰기 | 관련 테스트·우선도 |
|---|---|---|---|---|---|
| F01 앱 시작 → 저장된 인증 상태 복원 → 사용자 확인 → 진입 화면 결정<br>콜드 스타트, 세션 만료, 저장소 읽기 실패, 초기 요청 실패를 포함한다. | SplashScreen/useEnterApp → session/entry → GET me | 저장 토큰·User | 양 모드 | 없음 | auth/session/entry; 높음 |
| F02 회원가입·로그인 → 토큰 저장 → 보호 API 이용 → 세션 갱신 → 로그아웃·전체 로그아웃<br>사용자 상태 변경, 동시 401, 갱신 중 로그아웃·다른 사용자 로그인도 포함한다. | Login/Signup/useLogout → AuthController → AuthService/AccessTokenGuard | User·RefreshTokenSession | 양 모드 | 인증 기록 | auth/session/auth.integration; 높음 |
| F03 현재 시즌·시즌 목록 조회 → 참가 가능 여부 판단 → 시즌 참가<br>시즌 계정·초기 지갑·초기 자금·원장·성과 기준 생성과 중복 참가를 포함한다. | SeasonJoin → SeasonsController.join → SeasonsService | Season·SeasonParticipant·TradingAccount·CashWallet·WalletTransaction·EquitySnapshot | 시즌 | 있음 | seasons.join/trading-account; 높음 |
| F04 일반모드 진입 → 일반계정 개설 → 초기 지갑·자금·원장·성과 기준 생성<br>중복 개설 요청, 기존 일반계정 재진입, 조회와 개설의 경계를 포함한다. | ModeSelection/useOpenGeneralAccount → TradingAccountsController → GeneralAccountsService | TradingAccount·CashWallet·WalletTransaction·EquitySnapshot | 일반 | 있음 | general-account/generalAccountOpen; 높음 |
| F05 계정 목록 조회 → 일반·시즌·과거 시즌 계정 선택 → 화면 조회 → 계정 전환<br>사용자별 선택 저장, 계정별 캐시, 진행 중 요청, 견적·주문·환전 상태 초기화를 포함한다. | TradingAccountContext → account-scoped API → TradingAccountAccessService | User↔TradingAccount·선택 저장·Query cache | 양 모드 | 없음 | accountSelection/accountScope/quotedAction; 높음 |
| F06 마켓 진입 → 자산 목록·검색·필터·페이지 조회 → 가격·등락률 표시<br>종목 식별자, 활성 종목, 통화·가격 단위, 가격 미제공 상태를 포함한다. | MarketScreen → AssetsController → AssetsService + useMarketTickers | Asset·AssetPriceSnapshot | 양 모드/국내·미국·crypto | 없음 | assets/marketTickerStore; 중간 |
| F07 자산 상세 진입 → 현재가·호가·보유 종목 조회 → 매수·매도 화면 이용<br>현재의 화면 내 주문 패널과 별도 진입 경로, 계정·종목 전환을 포함한다. | AssetDetail/OrderPanel/OrderScreen → assets/positions/orders | Asset·Position·Quote | 양 모드/시장별 시간·통화 | 견적 | assetDetail/tradingUx; 높음 |
| F08 차트 진입 → 기간·봉 간격 선택 → 과거 데이터 조회 → 실시간 반영<br>전체화면 차트, 이동·확대·선택, 화면 이탈·복귀, HTTP와 실시간 데이터 합성을 포함한다. | AssetChart/useAssetCandle → AssetCandlesService + candle channel | MarketCandle·SyncState·캐시 | 양 모드/주식·crypto | 없음 | asset-candles/liveCandle; 중간 |
| F09 KIS·Binance 시세 수신 → 검증·정규화 → 저장·전파 → 화면·평가·거래에서 선택<br>구독 범위, 중복 수신, 순서 역전, 저장 제한, 공급자 장애, 등락률 기준을 포함한다. | ProvidersModule → KIS/Binance ingestion/live stream → snapshot + realtime | AssetPriceSnapshot·OrderbookSnapshot | 양 모드/공급자별 | 시세만 | providers/market-data; 높음 |
| F10 시장 캘린더 판정 → 개장·휴장 상태 → 적격 가격 선택 → 휴장 가격 유지·종가 복구<br>주말·공휴일·특수 세션·연도 경계·시간대·운영자 override를 포함한다. | market-calendar registry/override loader → source policy → closed-price recovery | 캘린더 코드·MarketSessionOverride·snapshot | 양 모드/KRX·US·crypto | 시세·override | market-calendar/krx-closed-price; 높음 |
| F11 공유 WebSocket 연결 → 인증 → 채널 구독 → 메시지 수신 → 재연결·재동기화·구독 해제<br>현재가·캔들·호가·환율 네 채널, 세션 변경, 화면 반복 이동, 느린 클라이언트를 포함한다. | shared market socket → AssetTickerGateway → ticker/candle/orderbook/fx | 토큰·프로세스별 subscription·Redis cache | 양 모드 | 없음 | asset-ticker.gateway/sharedRealtimeSocket; 높음 |
| F12 캔들 초기 수집·증분 수집 → DB 저장 → 파생 봉 집계 → 캐시 → API 서빙<br>데이터 완전성, 체크포인트, 요청 시 갱신, 보정, 보존기간 삭제, 장애 후 재개를 포함한다. | candle schedulers + GET candles → sync/store/aggregate/cache/retention | MarketCandle·MarketCandleSyncState | 양 모드/공급자별 | 시세만 | candle fixture/candle-*; 높음 |
| F13 계정 지갑·사용 가능 잔액 조회 → 원장 조회 → 통화·유형·방향 필터 → 페이지 이동<br>예약금, 실제 balanceAfter, 주문 메타데이터, 거래 수량, 다른 계정 참조 차단을 포함한다. | WalletTransactions/Portfolio → WalletsController/account controller → WalletsService | CashWallet·WalletTransaction·Order | 양 모드/KRW·USD | 없음 | wallets/transactions; 높음 |
| F14 환율 공급자 조회 → observation 저장 → 적격 환율 선택 → 현재 환율 표시·갱신·전파<br>EXIM 실패 후 대체 공급자 호출, 동시 갱신 병합, 환율 장애와 지갑 장애 분리를 포함한다. | GET fx/rates/current + provider job → FX refresh/source selection → fx channel | FxRateSnapshot | 양 모드/KRW↔USD | 관측만 | fx-refresh/fxRateUpdates; 높음 |
| F15 시장가 매수·매도 입력 → 견적 → 주문 생성·실행 → 지갑·포지션·원장·성과 반영<br>재견적, 수수료, 정밀도, 중복 클릭·재시도, 응답 유실과 거래 후 화면 갱신을 포함한다. | OrderPanel → quote/create → OrdersService → financial transaction | Quote·Order·Wallet·Position·Ledger·Equity | 양 모드/시장별 정밀도 | 있음 | orders.execute/general-account-trading; 높음 |
| F16 지정가 매수·매도 견적 → 등록 → 현금 또는 보유수량 예약 → 미체결 조회<br>예약 중 다른 매매·환전, 동일 계정 동시 주문, 기능 플래그와 재요청을 포함한다. | OrderPanel → limit quote/create → LimitOrderCreateService | Quote·Order·cash/position reservation | 양 모드/매수·매도 | 있음 | limit-order-reservation/create-race; 높음 |
| F17 지정가 매칭 주기 → 후보 탐색 → 경로 A/B 판단 → 체결 → 예약 해제·성과 반영<br>매수·매도, 후보 탐색 공정성, 오래된 미체결, 시간 경계, 가격 근거, 반복 실패를 포함한다. | OpsScheduler limit interval → MatchingService → CandidateRepository → ExecutionService | Order·snapshot/candle evidence·금융 데이터 | 양 모드/매수·매도 | 있음 | limit-order-matching; 높음 |
| F18 주문 취소·참가자 제외·시즌 종료 정리 → 주문 상태 확정 → 예약금·예약수량 해제<br>체결과 취소, 정리 작업과 신규 주문이 동시에 발생하는 경우를 포함한다. | cancel API + moderation/lifecycle → LimitOrderCancelService | Order·CashWallet·Position | 양 모드/시즌 정리 | 있음 | limit-order-cancel/replay-and-cancel; 높음 |
| F19 KRW↔USD 환전 입력 → 견적 → 실행 → 두 지갑·두 원장·환전 기록·성과 반영<br>실행 시 재가격, 견적 만료, 멱등 재요청, 예약금 침범 방지, 계정 전환을 포함한다. | WalletFxScreen → account FX quote/execute → FxService | Quote·FxExecuteRequest·ExchangeTransaction·Wallet·Ledger·Equity | 양 모드/KRW↔USD | 있음 | fx.execute/general-account-fx; 높음 |
| F20 포지션·포트폴리오·홈 조회 → 가격·환율 선택 → 총자산·손익·수익률 표시<br>일반계정 시간가중 수익률과 외부 자금, 시즌 성과, 부분 장애와 무결성 오류를 포함한다. | Home/Portfolio → account portfolio/positions/wallets → valuation/performance | Wallet·Position·price/FX·EquitySnapshot | 양 모드/TWR·시즌수익률 | 없음 | portfolio/general-performance/home; 높음 |
| F21 광고 보상 자격 조회 → 외부 검증 → 청구 처리 → 일반계정 입금·원장·성과 경계 기록<br>공급자 미연결, 중복 증명, 지급 한도, 재요청, 거래와 보상의 동시 실행을 포함한다. | account ad-rewards endpoints → AdRewardService → verifier | AdRewardClaim·Wallet·Ledger·Equity boundary | 일반 | 있음 | ad-reward/general-performance; 높음 |
| F22 일별 스냅샷 대상 확인 → 일반·시즌 계정 평가 → 저장 → 홈 일별 추이 표시<br>늦은 참가·신규 계정, 기존 행 건너뛰기, 실패 재시도, 날짜 변경, 중복 실행을 포함한다. | daily snapshot jobs → account/participant valuation → daily equity UI | DailyPortfolioSnapshot·EquitySnapshot | 양 모드 | 성과 기록 | general-daily/daily-portfolio-snapshot; 높음 |
| F23 거래·정기 작업에 따른 현재 랭킹 갱신 → 랭킹 목록·내 순위 조회<br>전체 참가자 평가, 정렬·동률, 숨김·제외, 페이지 간 일관성, 갱신 중 조회를 포함한다. | trade/FX/limit fill + scheduler → RankingRefreshService → RankingController | SeasonRanking·Portfolio·EquitySnapshot | 시즌 | 랭킹 | ranking/season-ranking-scope; 높음 |
| F24 시즌 종료 → 미체결·예약 정리 → 최종 평가·랭킹 확정 → 계정 종료·등급 확정<br>종료 직전 거래, 정산 중 실패·재실행, 최종 결과 불변성, 제외된 계정 처리를 포함한다. | lifecycle → settlement → tier jobs → records | Season·Participant·Account·Ranking·Equity | 시즌 | 확정 성과 | season-settlement/final-tier; 높음 |
| F25 전적·과거 시즌·주문·환전·자산 추이 조회 → 공개 사용자 요약 표시<br>현재 데이터와 확정된 과거 결과의 구분, 권한, 캐시 형태, 페이지 조회를 포함한다. | RecordStack/UserSeasonSummary → RecordsController → RecordsService | Order·Exchange·Equity·final/current ranking | 시즌 전적 + 계정 주문 | 없음 | records/accountOrders/queryCache; 중간 |
| F26 시즌 보상·배지 생성 → 지급 요청 → 처리·실패·재시도·취소 → 사용자 표시<br>내부 보상 상태와 실제 외부 지급 여부, 중복 지급 방지, 운영자 권한을 포함한다. | reward grant job → operator fulfillment → RewardScreen | SeasonReward·Badge·UserBadge·RewardFulfillmentRequest | 시즌 | 보상 상태 | rewards/reward-fulfillment; 중간 |
| F27 관리자·운영자 인증 → 사용자·참가자 상태 변경·복구·결과 보정 → 감사 기록<br>시장 캘린더 수정, 수집·배치 수동 실행, 다른 흐름에 대한 상태 변경 효과를 포함한다. | operator controllers/guard → account/moderation/override/provider/batch services | User·Participant·Override·AuditLog | 양 모드/역할별 | 경로별 | operator/ops/reward fulfillment; 높음 |
| F28 서버 시작·설정 검증 → 수집기·스케줄러 시작 → 잠금·작업 기록 → 종료·재시작·복구<br>Render 운영 전제, DB·Valkey 장애, 잠금 만료·갱신, 배포 중 중복 실행, 데이터 누적을 포함한다. | bootstrap/env-validation → OpsScheduler/Runner/Lock + batch CLI/readiness | OpsJobLock·OpsJobRun·BatchJobRun·DB/Redis | 모든 흐름 | job별 | env-validation/ops-job-lock; 높음 |
| F29 일반모드 가이드·MY·설정 등 보조 화면 진입 → 탐색 → 계정·세션 변화 후 복귀<br>실제 연결된 기능과 화면 수명주기는 확인하되 정적 문구·장식 개선에 시간을 쓰지 않는다. | GuideStack/My/Settings + PATCH me → auth/account navigation | 사용자 프로필·세션·선택 계정 | 양 모드 | 프로필만 | guide/MainTabs/session; 중간 |

외부 의존성: F01~F05/F13/F15~F27은 API·DB, F06~F12는 KIS/Binance·DB·선택적 Redis, F14/F19/F20~F24는 USD 노출 시 EXIM/ExchangeRate API, F21은 검증 공급자(기본 미연동), F26은 내부 지급 상태 기록(외부 송금 adapter 미연동), F28은 DB·Valkey·호스팅 lifecycle이다.

설정 조사 연결: F01~F05 JWT/API URL; F06~F12 provider/realtime/candle/calendar; F15~F18 LIMIT_ORDER_ENABLED·SCHEDULER_LIMIT_ORDER_MATCHING_ENABLED 및 frontend 공개 flag; F19 general FX fee; F21 AD_REWARD_ENABLED/verifier; F22~F24/F26/F28 scheduler global/job/dry-run 조합. 코드 기본값과 실제 배포값을 구분하여 본 보고서 G에 기록한다.
